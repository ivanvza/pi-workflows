# Private workflows — not included in the package
.pi/workflows/*.js

# Persisted workflow run state (runtime data, one JSON file per run)
.pi/workflow-runs/

# Local project guidance (not shipped, not tracked)
/CLAUDE.md
/.claude/

# Git clone artifacts
.pi/git/
.pi/npm/

# OS
.DS_Store
/cloud-models.md
/cloud-models-summary.md
